<?xml version="1.0" encoding="UTF-8"?>
<OfficeApp xmlns="http://schemas.microsoft.com/office/appforoffice/1.1" xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance" xmlns:bt="http://schemas.microsoft.com/office/officeappbasictypes/1.0" xmlns:ov="http://schemas.microsoft.com/office/taskpaneappversionoverrides" xsi:type="ContentApp">

  <!-- Begin Basic Settings: Add-in metadata, used for all versions of Office unless override provided. -->

  <!-- IMPORTANT! Id must be unique for your add-in, if you reuse this manifest ensure that you change this id to a new GUID. -->
  <Id>87654321-4321-4321-4321-210987654321</Id>
  <Version>1.0.0.0</Version>
  <ProviderName>PowerPoint Quiz Controller</ProviderName>
  <DefaultLocale>en-US</DefaultLocale>
  <!-- The display name of your add-in. Used on the store and various places of the Office UI such as the add-ins dialog. -->
  <DisplayName DefaultValue="Quiz Dashboard" />
  <Description DefaultValue="Real-time quiz dashboard showing player status and click events." />
  <!-- IconUrl must be a HTTPS URL. -->
  <IconUrl DefaultValue="https://localhost:8080/powerpoint-addin/icon-32.png" />
  <HighResolutionIconUrl DefaultValue="https://localhost:8080/powerpoint-addin/icon-64.png" />
  <SupportUrl DefaultValue="https://localhost:8080" />
  <!-- Domains that will be allowed when navigating to them from the add-in -->
  <AppDomains>
    <AppDomain>https://localhost:8080</AppDomain>
  </AppDomains>
  <!--End Basic Settings. -->

  <!--Begin TaskPane Mode integration. This section is used if there are no VersionOverrides or if the Office client version does not support add-in commands. -->
  <Hosts>
    <Host Name="Presentation" />
  </Hosts>
  <DefaultSettings>
    <SourceLocation DefaultValue="https://localhost:8080/powerpoint-addin/content.html" />
  </DefaultSettings>
  <!-- End TaskPane Mode integration.  -->

  <Permissions>ReadWriteDocument</Permissions>

  <!-- Begin Add-in Commands Mode integration. -->
  <VersionOverrides xmlns="http://schemas.microsoft.com/office/taskpaneappversionoverrides" xsi:type="VersionOverridesV1_0">

    <!-- The Hosts node is required. -->
    <Hosts>
      <!-- Each host can have a different set of commands. -->
      <!-- Excel host is Workbook, Word host is Document, and PowerPoint host is Presentation. -->
      <!-- Make sure the hosts you override match the hosts declared in the top level OfficeApp element. -->
      <Host xsi:type="Presentation">
        <!-- Form factor. Currently only DesktopFormFactor is supported. -->
        <DesktopFormFactor>
          <!--"This code enables a customizable task pane. -->
          <GetStarted>
            <!-- Title of the Getting Started callout. resid points to a ShortString resource -->
            <Title resid="GetStarted.Title"/>

            <!-- Description of the Getting Started callout. resid points to a LongString resource -->
            <Description resid="GetStarted.Description"/>

            <!-- Point to a url resource which details how the add-in should be used. -->
            <LearnMoreUrl resid="GetStarted.LearnMoreUrl"/>
          </GetStarted>
          <!-- Function file is a HTML page that includes the JavaScript where functions for ExecuteAction will be called. 
            Think of the FunctionFile as the code behind ExecuteFunction. -->
          <FunctionFile resid="Commands.Url" />

          <!-- PrimaryCommandSurface is the main Office Ribbon. -->
          <ExtensionPoint xsi:type="PrimaryCommandSurface">
            <!-- Use OfficeTab to extend an existing Tab. Use CustomTab to create a new tab. -->
            <OfficeTab id="TabHome">
              <!-- Ensure you provide a unique id for the extension. -->
              <Group id="CommandsGroup">
                <!-- Label for your group. resid must point to a ShortString resource. -->
                <Label resid="CommandsGroup.Label" />
                <!-- Icons. Required sizes 16,32,80, optional 20, 24, 40, 48, 64. -->
                <!-- Use PNG icons. All URLs on the resources section must use HTTPS. -->
                <Icon>
                  <bt:Image size="16" resid="Icon.16x16" />
                  <bt:Image size="32" resid="Icon.32x32" />
                  <bt:Image size="80" resid="Icon.80x80" />
                </Icon>

                <!-- Control. It can be of type "Button" or "Menu". -->
                <Control xsi:type="Button" id="ContentButton">
                  <Label resid="ContentButton.Label" />
                  <Supertip>
                    <!-- ToolTip title. resid must point to a ShortString resource. -->
                    <Title resid="ContentButton.Label" />
                    <!-- ToolTip description. resid must point to a LongString resource. -->
                    <Description resid="ContentButton.Tooltip" />
                  </Supertip>
                  <Icon>
                    <bt:Image size="16" resid="Icon.16x16" />
                    <bt:Image size="32" resid="Icon.32x32" />
                    <bt:Image size="80" resid="Icon.80x80" />
                  </Icon>

                  <!-- This is what happens when the command is triggered (E.g. on the press of the Ribbon). -->
                  <!-- Supported actions are ExecuteFunction or ShowTaskpane. -->
                  <Action xsi:type="ShowTaskpane">
                    <TaskpaneId>ButtonId2</TaskpaneId>
                    <!-- Provide a url resource id for the location that will be displayed on the task pane. -->
                    <SourceLocation resid="Content.Url" />
                  </Action>
                </Control>
              </Group>
            </OfficeTab>
          </ExtensionPoint>
        </DesktopFormFactor>
      </Host>
    </Hosts>

    <!-- You can use resources across hosts and form factors. -->
    <Resources>
      <bt:Images>
        <bt:Image id="Icon.16x16" DefaultValue="https://localhost:8080/powerpoint-addin/icon-16.png" />
        <bt:Image id="Icon.32x32" DefaultValue="https://localhost:8080/powerpoint-addin/icon-32.png" />
        <bt:Image id="Icon.80x80" DefaultValue="https://localhost:8080/powerpoint-addin/icon-80.png" />
      </bt:Images>
      <bt:Urls>
        <bt:Url id="GetStarted.LearnMoreUrl" DefaultValue="https://localhost:8080" />
        <bt:Url id="Commands.Url" DefaultValue="https://localhost:8080/powerpoint-addin/commands.html" />
        <bt:Url id="Content.Url" DefaultValue="https://localhost:8080/powerpoint-addin/content.html" />
      </bt:Urls>
      <!-- ShortStrings max characters==125. -->
      <bt:ShortStrings>
        <bt:String id="GetStarted.Title" DefaultValue="Get started with your quiz dashboard!" />
        <bt:String id="CommandsGroup.Label" DefaultValue="Quiz Dashboard" />
        <bt:String id="ContentButton.Label" DefaultValue="Quiz Dashboard" />
      </bt:ShortStrings>
      <!-- LongStrings max characters==250. -->
      <bt:LongStrings>
        <bt:String id="GetStarted.Description" DefaultValue="Your quiz dashboard add-in loaded successfully. Go to the HOME tab and click the 'Quiz Dashboard' button to show the real-time player table." />
        <bt:String id="ContentButton.Tooltip" DefaultValue="Click to Show Quiz Dashboard" />
      </bt:LongStrings>
    </Resources>
  </VersionOverrides>
  <!-- End Add-in Commands Mode integration. -->

</OfficeApp>
